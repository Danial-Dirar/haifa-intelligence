# Copy to .env.local and adjust. Never commit .env.local (it is gitignored).

# ComfyUI endpoint used by the AI Studio to generate images.
#   Local dev: your home ComfyUI server.
#   Production: the Cloudflare Tunnel URL that fronts the home GPU.
COMFYUI_URL=http://127.0.0.1:8188
